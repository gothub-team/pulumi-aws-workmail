name: provider-aws-workmail-native
runtime: yaml
plugins:
  providers:
    - name: aws-workmail
      path: ../../bin

resources:
  myRandomResource:
    type: aws-workmail:Random
    properties:
      length: 24

outputs:
  output:
    value: ${myRandomResource.result}
